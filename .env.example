# -------------------------------
# DATABASE CONFIGURATION
# -------------------------------

# Development DB connection (local PostgreSQL or cloud if needed)
DATABASE_URL=postgresql://your_user:your_password@localhost:5432/your_database

# Example Neon (production): 
# DATABASE_URL=postgresql://user:password@your-db.neon.tech/dbname?sslmode=require


# -------------------------------
# SERVER CONFIGURATION
# -------------------------------

# Port where the server will run (default 3001)
PORT=3001

# Environment: development | production
NODE_ENV=development

# Optionally, if using JWT or Auth:
# JWT_SECRET=your_super_secret_key
# TOKEN_EXPIRES_IN=1d


# -------------------------------
# ADVANCED (OPTIONAL)
# -------------------------------

# If you're deploying with Vercel, you should:
# - Move secrets like DATABASE_URL and JWT_SECRET to the Vercel dashboard (Environment Variables).
# - Set NODE_ENV=production in Vercel settings, not in this file.

# CORS setup (if you need to control frontend origin):
# CORS_ORIGIN=http://localhost:3000

# Enable logging
# LOG_LEVEL=info
